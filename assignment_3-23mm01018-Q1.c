#include<stdio.h>
int main()
{
    int a;
   printf("Enter any integer", a);
   scanf("%d",&a);
   if (a>0)
   {
    printf("Integer a is positive",a);
   }
    else if (a==0)
    {
printf("the given integer is zero",a);
    }
    else
   { printf("the given integer is negetive",a);
   }
    return 0;
}
